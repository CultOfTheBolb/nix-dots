{
  programs.nvf.settings.vim.treesitter = {
    enable = true;

    addDefaultGrammars = true;
    autotagHtml = true;
    fold = true;

    highlight = {
      enable = true;
    };

    incrementalSelection = {
      enable = true;
    };

    indent = {
      enable = true;
    };

    textobjects = {
      enable = true;
    };
  };
}
